spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  boot:
    admin:
      client:
        enabled: false
      server:
        enabled: false
  jmx:
    enabled: false
  application:
    admin:
      enabled: false
  security:
    user:
      name: admin
      password: admin
management:
  metrics:
    export:
      jmx:
        enabled: false

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

